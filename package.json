{
  "name": "Core-Exchange-Spec",
  "version": "5.0.0",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.4",
    "@stoplight/spectral-cli": "^6.3.0",
    "bump-cli": "^2.4.1",
    "handlebars": "^4.7.7",
    "husky": "^7.0.0",
    "yaml": "^2.2.2"
  },
  "scripts": {
    "build": "sh ./scripts/build-codegen.sh",
    "clean": "rm -rf ./openapi-build",
    "prepare": "husky install"
  },
  "dependencies": {
    "ts-node": "^10.8.1"
  }
}
